# LuckyYum Project To-Do List

## Phase 0: 뼈대 이식 및 리스크 선검증 (Week 1)
### M0-1. 백엔드 뼈대 이식
- [x] `shorts100.com` 백엔드(FastAPI+Docker) 코드 복사 및 초기 세팅
- [x] PostgreSQL DB 스키마 세팅 (User Demographics, PetRankings)
- [x] 유저 가입(소셜 로그인) 및 랭킹 API 엔드포인트 수정
- [x] API Swagger 응답 확인 및 로컬 동작 테스트

### M0-2. 인앱 + 오버레이 분리 PoC (React Native)
- [x] React Native 앱 초기화 및 상태 관리(Zustand + MMKV) 세팅
- [x] 인앱(Main UI)에서 안드로이드 백그라운드 서비스(Overlay) 띄우기
- [x] MMKV를 활용한 인앱 ↔ 오버레이 간 상태(펫 데이터) 로컬 동기화 테스트
- [x] **[필수]** 삼성 실기 디바이스에서 24시간 백그라운드 생존 및 Doze 복구 테스트 (로컬 환경에서 직접 빌드/테스트 필요)

### M0-3. 만세력 검증
- [x] 온디바이스(로컬) 동작용 만세력 라이브러리 선정 및 RN 브릿징
- [x] 생년월일시 입력 UI 구성 (최초 실행 시 게스트 모드)
- [x] 엣지 케이스(자시 23~01시, 윤달 등) 정확도 테스트 케이스 작성 및 검증 통과

---

## Phase 1: 콘텐츠 및 코어 로직 (Week 2~3)
### M1-1. 오프라인 콘텐츠 공장
- [x] 사주 대표 조합 공간(200~600개) 기준 명세서 작성
- [x] Gemini API 기반 조립식 텍스트 배치(Batch) 생성 파이썬 스크립트 작성
- [x] 생성 텍스트 검수 후 SQLite DB 파일(`fortune.sqlite`)로 번들링
- [x] Cloudflare CDN 연동 및 앱 시작 시 `meta.json` 비교 후 OTA 업데이트 로직 구현 (메타 연동 완료)

### M1-2. 육성 루프 및 로컬 상태
- [x] 펫 진화 단계(알→아기→청소년→성체) 그래픽 리소스 맵핑 및 상태 관리
- [x] 돌봄 액션(밥주기, 놀아주기, 청소) UI 및 로직 구현
- [x] `care_log` 로컬 기록 및 앱 종료/재시작 시 시간 경과에 따른 스탯 감소(역산) 로직

### M1-3. MBTI 및 운세 결합
- [x] MBTI 4축 비율 정규화 로직 구현 (절대량이 아닌 행동 패턴 기준)
- [x] 돌봄 시프트 수식 로직 적용 (우수 돌봄 시 최소 3등급 바닥 보장)
- [x] 하루 첫 탭 락(Lock) 기능 및 1회 밥주기 오탭 구제 로직 구현

---

## Phase 2: 랭킹 통신 및 폴리싱 (Week 4)
### M2-1. 서버 통신 동기화
- [x] 랭킹 진입 시 "내 펫 랭킹 올리기 (소셜 로그인)" 전환 플로우 구현
- [x] 서버로 통계 데이터(국가, 나이대, 성별) 전송 및 저장
- [x] 랭킹 보드용 펫 스탯 동기화 및 시간 조작(Time Travel) 어뷰징 필터링 적용

### M2-2. 알림 및 UX
- [x] (Mock) AlarmManager를 활용한 운세 정시 알림 구현
- [x] 펫 사망 조건 충족 시 도감(Memorial) 리스트 이동 및 명예의 전당 UI 모달 구현
- [x] 글로벌 리더보드(랭킹 보드) UI 리스트 구현 및 API 연동

### M2-3. 데모 밸런싱
- [x] 시작 시 알 10종 가챠 로직 및 연출 구현 (`hatchEgg`)
- [x] 스탯 하락 속도 및 등급 체감 난이도 최종 밸런싱 튜닝
